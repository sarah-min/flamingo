# flamingo

### setup instructions
* (on first setup) run `python -m venv .venv`
* (when you open a new terminal) run `.venv\Scripts\activate` on Windows and `source .venv/bin/activate` on MacOS/Linux
* run `pip install -r requirements.txt `